# Wi-Vi Sentinel — local configuration
# Copy this file to .env and edit as needed.
# .env is gitignored; .env.example is the committed reference.

# ── Flask API server ──────────────────────────────────────────────────────────
FLASK_PORT=5555

# ── Vite dev server ───────────────────────────────────────────────────────────
VITE_PORT=3000

# ── CSI source ────────────────────────────────────────────────────────────────
# "simulated" (default) or "nexmon" (live from Raspberry Pi 4)
CSI_SOURCE=simulated

# ── Nexmon / UDP ─────────────────────────────────────────────────────────────
# UDP port the Pi streams CSI frames to (must match Pi config.json udp_port)
CSI_UDP_PORT=5500

# ── Device probe sniffing ─────────────────────────────────────────────────────
# Monitor-mode interface to sniff 802.11 probe requests.
# Leave blank to disable probe sniffing (mDNS still runs automatically).
#
# Common values:
#   PROBE_IFACE=          ← disabled (default)
#   PROBE_IFACE=mon0      ← Nexmon monitor interface (most common)
#   PROBE_IFACE=wlan0     ← NIC in direct monitor mode (no separate mon interface)
#   PROBE_IFACE=wlan0mon  ← alternative naming (check with: iw dev)
PROBE_IFACE=
